**fetch**는 **데이터를 가져오는(fetch) 함수 또는 동작**을 의미합니다. 웹 개발에서는 보통 **서버에 요청(Request)을 보내고 응답(Response)을 받아오는 API**를 말합니다.

가장 흔한 예는 JavaScript의 `fetch()`입니다.

이 과정은 다음과 같습니다.

1. 서버에 요청을 보낸다.
2. 서버가 데이터를 반환한다.
3. 응답을 원하는 형태(JSON, 텍스트, Blob 등)로 읽는다.