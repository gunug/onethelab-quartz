SSH는 중간자 공격(Man-in-the-Middle Attack)을 방지하기 위해 서버의 공개키를 기억합니다.

첫 번째 접속:

- 서버의 공개키를 받은 적이 없음
- 따라서 신뢰 여부를 사용자에게 묻습니다.

`yes`를 입력하면

```
~/.ssh/known_hosts
```

(Windows에서는 `%USERPROFILE%\.ssh\known_hosts`)에 이 서버의 공개키가 저장됩니다.

다음부터는 같은 서버에 접속할 때 이 메시지가 나타나지 않습니다.