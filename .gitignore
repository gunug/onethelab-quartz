# private notes (never publish)
02_private/

# editor / vault config
.obsidian/
.vscode/
.claudian/

# Quartz build artifacts & deps
quartz/node_modules/
quartz/public/
quartz/.quartz-cache/

# Supabase CLI local state (contains DB password in .temp — never commit)
supabase/.temp/
supabase/.branches/
supabase/config.toml
